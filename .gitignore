# Dependencies
node_modules
.next
.pnpm-debug.log

# Environment files（絕對不能進 git）
.env
.env.local
.env.production
.env.*.local

# 資料庫與上傳檔（VPS 上獨立存在 /opt/poshtech/data & uploads）
data/*.db
data/*.db-shm
data/*.db-wal
public/uploads/*
!public/uploads/.gitkeep
!public/uploads/logo.*
!public/uploads/placeholder.*
!public/uploads/banner-*.svg
!public/uploads/about.jpg
!public/uploads/contact-*.jpg
!public/uploads/cat-*.jpg
!public/uploads/prod-*.jpg

# 不 commit 預抽取的 pdftext（檔案大、可重建）
# 若需要部署時含有，請拿掉下面這一行註解
# data/pdftext/

# 型錄資料（僅本機使用，不部署到網站）
型錄/

# 內部文件（工作日誌、使用說明書、技術文件等 — 不部署）
docs/

# Claude Code 工作目錄
.claude/

# 編輯器/作業系統
.DS_Store
.vscode/*.log
*.swp
*~

# Logs
logs/
*.log
